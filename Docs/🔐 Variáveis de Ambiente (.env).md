
Arquivo `.env` (não versionado) exemplo:

```text
DB_USER=dev_user
DB_PASSWORD=dev_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_analytics
```

**Observação:** As credenciais devem coincidir com as definidas no `docker-compose.yml`.